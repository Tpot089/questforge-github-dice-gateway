name: QuestForge Dice Gateway

on:
  issues:
    types: [opened]

permissions:
  contents: write
  issues: write

jobs:
  roll:
    concurrency:
      group: questforge-issue-${{ github.event.issue.number }}
      cancel-in-progress: false
    if: startsWith(github.event.issue.title, '[QF-ROLL]')
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Check out gateway
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Execute QuestForge roll gate
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          python questforge-runtime/scripts/durable_issue_roll.py "$GITHUB_EVENT_PATH" /tmp/questforge-receipt.md
      - name: Publish verified receipt
        if: success()
        env:
          GH_TOKEN: ${{ github.token }}
          ISSUE_URL: ${{ github.event.issue.html_url }}
        shell: bash
        run: gh issue comment "$ISSUE_URL" --body-file /tmp/questforge-receipt.md
      - name: Publish failure
        if: failure()
        env:
          GH_TOKEN: ${{ github.token }}
          ISSUE_URL: ${{ github.event.issue.html_url }}
        shell: bash
        run: |
          printf '%s\n' '### QuestForge execution failed' '' 'Execution is incomplete or publication failed. Inspect durable claim/result records and run logs. Do not reroll this request.' > /tmp/questforge-failure.md
          gh issue comment "$ISSUE_URL" --body-file /tmp/questforge-failure.md

